- Entrada de Datos:
radio = float(input("Ingresa el valor del radio: ")): Solicita al usuario que ingrese el valor del radio del círculo y lo convierte a un número de punto flotante 
(float).

- Cálculo del Área:
área = 3.1416 * radio ** 2: Calcula el área del círculo utilizando la fórmula donde 𝜋 se aproxima a 3.1416.

- Cálculo del Perímetro:
perímetro = 3.1416 * 2 * radio: Calcula el perímetro (circunferencia) del círculo utilizando la fórmula donde 𝜋 se aproxima a 3.1416.

- Impresión de Resultados:
print("El área del círculo es: " + str(área)): Convierte el resultado del cálculo del área a una cadena de texto y lo imprime junto con un mensaje.
print("El perímetro del círculo es: " + str(perímetro)): Convierte el resultado del cálculo del perímetro a una cadena de texto y lo imprime junto con un mensaje.

- Resultado: El programa calcula y muestra el área y el perímetro del círculo basándose en el valor del radio ingresado por el usuario.
